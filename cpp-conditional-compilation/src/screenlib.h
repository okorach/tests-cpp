#include <cstring>
#include <iostream>
using namespace std;

int output_message(char * message)
{
    if (strlen(message) > 10)
    {
        // TODO: Split message
        cout << message;
    }
    else
    {
        cout << message;
    }
    return 0;
}